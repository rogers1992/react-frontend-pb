services:
  - type: static
    name: paraiso-biker-web
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        sync: false
    redirects:
      - source: /*
        destination: /index.html
        status: 200